# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (nunca versionar segredos)
.env
.env.*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/migrations/dev.db*

# Armazenamento de arquivos (STORAGE_PATH).
# Local: ./private/storage | Railway: Volume montado (fora do repo).
# Inclui os documentos de filiação (LGPD) — jamais versionar.
/private/

# legado (uploads antigos, antes do STORAGE_PATH)
/public/uploads/
/storage/
